const myName = 'Samet';

if (myName === 'Samet') {
    console.log('Hello!');
}

let isLoggedIn = true;

if (isLoggedIn) {
    console.log('User is logged in!');
}

const enteredUserName = ''; // treated like it is false

if (enteredUserName) {
    console.log('Input is valid!');
}